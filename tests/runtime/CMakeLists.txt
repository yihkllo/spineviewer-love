add_executable(spinelove_runtime_contract runtime_contract.cpp)
target_link_libraries(spinelove_runtime_contract PRIVATE SpineLove::Runtime)
foreach(version 2.1 3.1 3.4 3.5 3.6 3.7 3.8 4.0 4.1 4.2)
    add_test(NAME runtime.memory.${version} COMMAND spinelove_runtime_contract ${version})
endforeach()
add_test(NAME runtime.all_versions_coexist COMMAND spinelove_runtime_contract)

add_executable(spinelove_runtime_legacy_smoke "${PROJECT_SOURCE_DIR}/main/runtime_v2/smoke.cpp")
target_link_libraries(spinelove_runtime_legacy_smoke PRIVATE SpineLove::Runtime)
add_test(NAME runtime.legacy_smoke COMMAND spinelove_runtime_legacy_smoke)

if(TARGET spinelove_sdk)
    add_executable(spinelove_playback_contract playback_contract.cpp)
    target_link_libraries(spinelove_playback_contract PRIVATE spinelove_sdk)
    target_include_directories(spinelove_playback_contract PRIVATE "${PROJECT_SOURCE_DIR}/main/runtime_v2")
    add_test(NAME playback.layers_and_renderer COMMAND spinelove_playback_contract)
endif()

if(TARGET spinelove_sdk AND TARGET spinelove_live2d_windows)
    set(SL_TEST_SHADER "SL_TEST_SHADER_PATH=\"${PROJECT_SOURCE_DIR}/main/render_d3d11/shaders/sprite.hlsl\"")

    add_executable(spinelove_live2d_bridge_contract live2d_bridge_contract.cpp)
    target_link_libraries(spinelove_live2d_bridge_contract PRIVATE spinelove_sdk)
    target_include_directories(spinelove_live2d_bridge_contract PRIVATE "${PROJECT_SOURCE_DIR}/main/qt/core")
    target_compile_definitions(spinelove_live2d_bridge_contract PRIVATE ${SL_TEST_SHADER})
    spinelove_test_target(spinelove_live2d_bridge_contract)
    add_test(NAME live2d.bridge_commands COMMAND spinelove_live2d_bridge_contract)
    set_tests_properties(live2d.bridge_commands PROPERTIES SKIP_RETURN_CODE 77)

    add_executable(spinelove_live2d_lifecycle live2d_lifecycle_contract.cpp)
    target_link_libraries(spinelove_live2d_lifecycle PRIVATE spinelove_sdk)
    target_compile_definitions(spinelove_live2d_lifecycle PRIVATE NOMINMAX ${SL_TEST_SHADER})
    spinelove_test_target(spinelove_live2d_lifecycle)

    add_executable(spinelove_asset_corpus asset_corpus.cpp)
    target_link_libraries(spinelove_asset_corpus PRIVATE spinelove_sdk spinelove_live2d_windows)
    target_include_directories(spinelove_asset_corpus PRIVATE "${PROJECT_SOURCE_DIR}/main")
    target_compile_definitions(spinelove_asset_corpus PRIVATE NOMINMAX ${SL_TEST_SHADER})
    spinelove_test_target(spinelove_asset_corpus)
endif()

add_executable(spinelove_unity_playback_test unity_playback_test.cpp)
target_link_libraries(spinelove_unity_playback_test PRIVATE nlohmann_json)
target_include_directories(spinelove_unity_playback_test PRIVATE "${PROJECT_SOURCE_DIR}/main")
target_compile_features(spinelove_unity_playback_test PRIVATE cxx_std_17)
spinelove_test_target(spinelove_unity_playback_test)
